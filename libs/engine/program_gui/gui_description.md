# GUI

The main idea of the program GUI is to use the frames data pass to the **SDL3** and draw everything - (UI and window with video) by the SDL3 methods.


So the main task is to translate **cv::Mat** type into the SDL_Texture type, like this guy already did:

https://stackoverflow.com/questions/22702630/converting-cvmat-to-sdl-texture